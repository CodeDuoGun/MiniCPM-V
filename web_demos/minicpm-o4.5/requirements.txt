# MiniCPM-o 4.5 has only been validated with this Transformers release.
# librosa==0.9.0 still imports pkg_resources. Setuptools 82 removed that module.
setuptools==80.9.0
transformers==4.51.0
# These three packages are one ABI-locked stack. Install them from the same
# official PyTorch CUDA index (cu126 or cu128) before the remaining packages.
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
accelerate
minicpmo-utils[all]>=1.0.5
Pillow>=10.1.0
# minicpmo-utils 1.0.5/1.0.6 pins librosa==0.9.0, which is not compatible
# with NumPy 2.x. Let minicpmo-utils install librosa and keep NumPy on 1.x.
numpy>=1.24,<2
soundfile>=0.12.1
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.10.0
aiofiles>=23.2.1
httpx>=0.27.0
prometheus-client>=0.21.0
dashscope>=1.25.17
peft>=0.14.0
redis>=5.0.0
qiniu>=7.13.0
